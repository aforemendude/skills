interface:
  display_name: 'Fix Tests & Styles'
  short_description: 'Fix CSS ownership, test coverage, and assertions'
  default_prompt: 'Use $fix-tests-styles to fix [CSS, unit tests, or both] in these files: [file paths].'
policy:
  allow_implicit_invocation: false
